// import { changeRole, getRoleByCcId, getUserDetails, login, logout } from "@/controllers/auth.controller";
// import { verifyToken } from "@/middlewares/auth.middleware";
// import { Router } from "express";

// export const authRouter = Router();

// authRouter.post("/login", login);
// authRouter.post("/change-role", verifyToken, changeRole);
// authRouter.post("/logout", verifyToken, logout);
// authRouter.post("/role-by-cc", verifyToken, getRoleByCcId);
// authRouter.post(
//   "/get-user-details",
//   verifyToken,
//   // authorize("pms:user:view"),
//   getUserDetails
// );

// export default authRouter;
