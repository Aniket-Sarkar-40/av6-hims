import { Router, type Router as ExpressRouter } from "express";
// import { listUsersController } from "../../controllers/auth/auth.controller";

export const authRoutes: ExpressRouter = Router();

// authRoutes.get("/users", listUsersController);
